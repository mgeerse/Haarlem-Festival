@model IEnumerable<HaarlemFestival_Web.Models.Subject>

@{
    Layout = null;
}


    @foreach (HaarlemFestival_Web.Models.Subject item in Model)
    {


        @Html.DisplayFor(m => item.Name)
        @Html.DisplayFor(m => item.Description )
    }
